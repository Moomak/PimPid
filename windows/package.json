{
  "name": "pimpid-windows",
  "version": "1.6.6",
  "description": "PimPid — Thai ⇄ English Keyboard Layout Converter for Windows",
  "main": "dist/main.js",
  "scripts": {
    "build": "tsc",
    "start": "npm run build && electron .",
    "dist": "npm run build && electron-builder --win portable",
    "dist:nsis": "npm run build && electron-builder --win nsis",
    "test": "vitest run",
    "simulate-article": "npm run build && npx electron dist/scripts/run-article-simulation.js"
  },
  "author": "PimPid",
  "license": "MIT",
  "devDependencies": {
    "@vitest/coverage-v8": "^4.1.0",
    "electron": "^33.0.0",
    "electron-builder": "^25.0.0",
    "typescript": "^5.6.0",
    "vitest": "^4.1.0"
  },
  "build": {
    "appId": "com.pimpid.windows",
    "productName": "PimPid",
    "npmRebuild": false,
    "win": {
      "target": [
        "portable"
      ],
      "icon": "build/icon.png"
    },
    "portable": {
      "artifactName": "PimPid-${version}-Windows.exe"
    },
    "files": [
      "dist/**/*",
      "src/icon.png",
      "src/settings.html",
      "src/onboarding.html",
      "src/toast.html",
      "src/float-button.html",
      "node_modules/uiohook-napi/**/*"
    ],
    "asarUnpack": [
      "**/node_modules/uiohook-napi/**/*"
    ],
    "directories": {
      "output": "release"
    }
  },
  "optionalDependencies": {
    "uiohook-napi": "^1.5.4"
  }
}
